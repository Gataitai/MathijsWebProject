function pixelArtPostCard(post){
    const id = post[0];
    const title = post[1];
    const pixelArtPostAsJson = post[2];
    const person = post[3];

    function template(){
        return
        `
        
        `
    }
}